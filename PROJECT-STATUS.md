# Samus Survivors — 專案進度交接文件

> **使用方式（2026-08-10 起改用 Claude Code）**：專案已整個搬進資料夾
> `C:\Users\kento\OneDrive\桌面\Samus Survivors`，並改用 Claude Code 直接在此資料夾內開發。
> 新開對話時只要說「讀 PROJECT-STATUS.md 接續開發」，Claude 就能自己讀取這份文件與最新程式碼，
> 不需要再手動上傳檔案。若改在其他環境（如網頁版聊天）接續，仍可把這份文件＋最新的
> `index.html` 一起上傳，效果相同。
> 建議每次開發到一個段落、或額度快用完前，請 Claude「更新這份交接文件」，保持內容是最新狀態。
>
> **測試／發佈方式（2026-08-10 起改為 git push 自動部署）**：遊戲主檔已改名為 `index.html`
> （GitHub Pages 首頁的必要命名），專案透過 git 推送到 GitHub repo，Pages 設定為從 main 分支
> 根目錄自動部署——每次 `git push` 後約 1 分鐘，線上版本就會自動更新，不再需要手動貼上程式碼。
> 每次交付新版仍要更新 `GAME_VERSION` 版本字串（見第 4 節），
> 讓使用者能從開始畫面左下角確認線上版本是否已更新。

---

## 1. 專案是什麼

一個以《超級銀河戰士》(Super Metroid) 世界觀、《吸血鬼倖存者》(Vampire Survivors) 玩法為核心的網頁小遊戲。
單人開發、單一 HTML 檔案（Canvas + 原生 JS，無外部框架），可直接雙擊在瀏覽器開啟遊玩，也可分享給朋友。

**核心賣點**：擊殺敵人升級 → 武器/被動能力進化 → 動力服外觀隨進化進度自動變色，
把「變強」與「變帥」直接綁在一起。

---

## 2. 目前已完成的部分

### 2.17 第十八輪（v1.8：武器與經驗晶石視覺重製）
> v1.7 驗收：「這次太棒了！全部感覺都很好！」；回饋晶石與武器效果和背景顏色相近難辨識。

- **可讀性設計原則（之後新特效一律遵守）**：背景是暗青綠色系，所有戰鬥元素採
  「暗描邊→色暈→彩色體→**白熱核心**」的分層結構，任何顏色在任何底色上都清楚；
  不用 shadowBlur（維持 2.2 節效能守則），發光感全靠透明度分層
- **彈幕四形狀重製**（bolt/shard/ribbon/star）＋飛彈（暗描邊彈體+白亮彈頭+雙層尾焰）
- **持續光束三層化**（寬暈+色體+白芯）＋命中點脈動光斑；移除 shadowBlur
- **環繞武器**：螺旋刀刃加白色刃緣、電漿球團加白芯
- **音波脈衝**：單細環改為「寬暈+色環+內側白環」三層
- **地雷**：暗描邊（萊姆綠在苔蘚上不明顯）＋紅白交替警示燈
- **經驗晶石重製**：全面改暖金色系（原本小晶石是青色`#4fd8ff`會融進背景）——
  一般=琥珀`#ffb347`、大顆=金`#ffe27a`，暗描邊+白芯+輕微脈動；整合結晶（紅八芒星）加暗描邊+白芯
- 實機截圖驗證：全部元素在新背景上清晰可辨，零錯誤

### 2.17b 第十八輪追加（v1.8.1：參考圖風格微調）
- 使用者提供特效參考圖（`ref/ultimate_swl...jpg`，像素技能特效合集），風格＝彗星型彈幕+鋸齒閃光
- bolt 彈幕改為**彗星型**（亮圓白頭+鋸齒漸細尾，尾緣以 `sin(age*28)` 顫動）
- 新增 `spawnImpactFlash()`：彈幕命中時的**鋸齒四芒星白色閃光**（0.13秒縮小消失，走粒子系統）
- 後續若要繼續套用參考圖風格：shard/star 也可比照彗星化；爆炸可改鋸齒放射狀

### 2.16 第十七輪（v1.7：背景美化＋頭目血量成長曲線）
> v1.6 驗收回饋：死亡演出OK、QUEEN目標達成；但「中頭目第2隻起反而比第一隻好打」
> 「QUEEN兩輪酸液就死了」——血量沒跟上玩家進化火力。

- **背景全面重製（俯視角地形，參考 ref/2740...jpg 的風格）**：捨棄舊的星空+遠山+格線側視背景，
  改為 VS 式俯視角地面。**整張 960×600 預渲染成離屏canvas**（每幀只做1次drawImage＋約19個
  微弱脈動光弧，效能成本趨近於零）。圖層：深藍綠漸層底→鵝卵石紋理（抖動網格橢圓石塊6色）→
  深色岩板區塊→苔蘚斑塊（隨機遊走綠點群）→綠水晶簇（含脈動微光）→灰岩塊→小草叢→
  紅色孢子點→暗角。調校記錄：脈動光第一版半徑×1.6/alpha 0.35 會變成灰白圓盤蓋住裝飾，
  改為×0.9/alpha≤0.13 後才自然
- **頭目血量成長曲線**：`startBossBattle` 依觸發當下的時間難度超線性放大
  `hp × max(1, diff.hp/2.8)^1.6`（5分Kraid×1為基準；10分Croco約×2.4→35萬、
  20分Ridley約×11→416萬）；QUEEN 公式×6（20分約540萬）。頭目連戰時延後登場的頭目
  也會按實際時間放大，天然適配
- **測試技巧（面板隱藏時的視覺驗證）**：瀏覽器面板被關閉時 rAF 停擺、無法截圖，
  可用 javascript_tool 將 canvas `toDataURL('image/jpeg',0.5)` 匯出 base64 →
  PowerShell 解碼成檔案 → Read 檢視。本輪背景即以此法迭代兩版

### 2.15 第十六輪（v1.6：QUEEN METROID 戰鬥重製＋巨型頭目死亡演出）
玩家回饋：中頭目死亡「只是消失太突然」；QUEEN「有時候被冷凍在畫面外就直接死掉」。

- **巨型頭目死亡演出**（含QUEEN）：血量歸零不再立即消失，改為1.6秒演出——連環小爆炸
  （每0.13秒隨機位置爆點+衝擊環）＋全身震動＋最後1秒淡出＋終場白色大爆炸，之後才移除、
  掉結晶、恢復生產。演出期間無敵、不再行動、接觸傷害關閉（`en.dying`）
- **QUEEN METROID 全面重製**（原本只有高血量+接觸傷害，且會被冰束凍在畫面外隔空打死）：
  - 改走巨型頭目系統（`giant:true`＋`bossBattle`）：從**頂部降臨**（進場無敵＋凍結免疫，
    根治凍卡場外bug）、放大2.1倍佔據畫面上方、頂部大血條、雜兵停產、死亡演出後才觸發勝利
  - 攻擊模式：**5發酸液扇射**（瞄準玩家）＋**全場擴張衝擊波**（環通過玩家時判定，8.5秒週期）＋
    **產卵 Metroid 幼體**（每6秒2隻、場上限6隻）
  - **Metroid 幼體**：新敵種（圖鑑取樣20x20，綠色圓頂+紅核+獠牙），飛行、接觸吸血
    （對玩家造成傷害並自我回血2.5倍），可被冰凍（呼應設計文件的冰系剋Metroid）——
    這也把待辦8「Metroid警報」的概念實現在QUEEN戰中
  - `entering` 進場邏輯泛化為任意方向（中頭目側向滑入、QUEEN頂部降下共用）
- 實測：死亡演出1.6秒完整播放、QUEEN裝冰束下滿血落地、酸液/衝擊波/產卵全循環、
  幼體上限6隻、擊殺→演出→勝利畫面，全程零錯誤
- 背景美化參考圖已收（`ref/2740...jpg`，深藍綠岩石地面+苔蘚+水晶的俯視風格），**下一輪動工**

### 2.14 第十五輪（v1.5：薩姆斯跑步動畫＋頭目動感）
> v1.4 驗收回饋：「終於玩到中頭目戰的感覺，很不錯」。本輪處理其餘回饋：

- **薩姆斯 10 幀跑步動畫**：使用者提供 SA-X sprite sheet（`ref/1704.png`，Metroid Fusion 素材），
  取第一列步行循環 10 幀，白背去背、鏡像成面向右、HSV 分類映射到動力服調色盤字母、
  統一 31×37 畫框底部對齊。移動時以約 15fps 循環播放（`walkPhase*1.5 % 10`），
  靜止時仍用原站姿矩陣。`SAMUS_RUN_UNIT` 讓跑步幀與站姿在畫面上同高。
  舊的「腿部偏移」假動畫已移除（被真幀取代）
- **中頭目動感**（回饋「如果中頭目也能動就好了」）：緩慢呼吸起伏（±1.6%，腳底錨定不滑動）＋
  攻擊瞬間朝戰場**前撲**（`lungeT` 0.25秒正弦衝程，四種頭目攻擊點都會觸發）
- **呼吸/揚塵調強**（回饋「沒有呼吸感」「揚塵要很仔細看」）：待機呼吸振幅 1.4%→3.5%（腳底錨定，
  頭部起伏明顯）；揚塵頻率近倍增、顆粒×2、更亮的沙色 `#c9b28a`
- 教訓：**用 PowerShell 的 -replace/Set-Content 改含中文的檔案會毀編碼**，改檔一律用
  Edit 工具或 Python（PowerShell 只做純 ASCII 替換）
- **v1.5.1 熱修**：玩家回報跑步動畫「一左一右交換」——根因是 sheet 第一列其實是
  「5幀朝右＋5幀朝左」兩段，當初對全部10幀統一鏡像反而做出方向交錯的循環。
  修法：逐幀偵測朝向（臂炮N像素質心 vs 身體質心）只鏡像朝左幀，並把逐幀 bbox 置中
  改成**身體質心對齊**（消除步行時的橫向抖動）。教訓：**sprite sheet 的同一列
  不保證同一朝向，取樣後必須逐幀驗證方向一致性**
- **v1.5.2 熱修**：跑步時變成 Varia 色、站定又變回黃色——SA-X 素材的主裝甲色相在 10~20 度
  （橘紅），被分類器歸到 R（primary），而站姿素材的裝甲是黃色（歸到 O）。修法：SA-X 專用
  分類閾值（真紅 hue<8 才是 R、8~45 亮色→O、暗色→W；深紫描邊维持近黑判定）。
  以「四套動力服 × 站姿+跑步」對照圖驗證全部一致（R佔比從大量降到3%）。
  教訓：**不同來源素材的取樣，字母分布必須跟既有素材比對**（同字母=同調色盤欄位，
  分布差異會讓同一套裝甲在不同動作下顯色不同）

### 2.13 第十四輪（v1.4：頭目戰依回饋大修）
針對 v1.3 試玩回饋（沒看到警告／血太少秒殺／放大後顆粒感重／只見到2隻頭目）：

- **警告改為全螢幕「威脅偵測」公告**（沿用 QUEEN 的 boss-announce 元件，動態換文字，1.8秒後頭目登場；
  QUEEN 公告時會把文字設回原樣）。原本的 suitBanner 小橫幅容易被升級選單蓋掉，已棄用
- **血量 ×16~17**（玩家原話「厲害20倍感覺都可以」）：80000／150000／250000／380000，
  擊破結晶總值 45→150
- **頭目高解析度重繪**：矩陣從 34~44 格寬提高到 84~120 格寬（Kraid 100×137、Croco 110×88、
  Phantoon 84×123、Ridley 120×86）、7色→12色量化（字母A~L），畫面上每格約3px，細節大幅回歸
  （紅眼、獠牙、鱗片、骨翼都清晰）。檔案增至約150KB，預渲染快取下效能無影響
- **「只看到2隻頭目」根因＝全進化太快觸發QUEEN結局**，Phantoon/Ridley根本沒機會登場。
  新機制**頭目連戰（boss rush）**：QUEEN 觸發時（全進化或25分保底）若還有中頭目未登場，
  會依序全部登場、全數擊破後 QUEEN 才現身。實測序列：保底→PHANTOON→RIDLEY→QUEEN→VICTORY 零錯誤
- **修復軟鎖**（測試中發現）：進化冰凍光束會把「進場中」的頭目永久凍在場外（無敵+雜兵停產=死局）。
  修法：進場處理移到凍結檢查之前；巨型頭目冰凍改為 0.8秒短凍結＋4秒免疫冷卻（防高射速冰束永凍）
- **薩姆斯動感包**（sprite-gen 評估後的原生替代方案）：待機呼吸（squash&stretch ±1.4%）、
  移動前傾 0.07rad、腳邊揚塵粒子——全部是繪製期變換，像素資料與動力服變色完全不動
- 中頭目觸發改經 `pendingMidBoss` 佇列（update() 消化），與 pendingFinalBoss 同款模式

### 2.12 第十三輪（v1.3：巨型頭目戰系統）
玩家構想（已確認）：中頭目佔滿畫面一側、專屬攻擊、戰鬥中停止生產雜兵。實作內容：

- **頭目戰流程**：時間到 → `startBossBattle(entry)`：警告橫幅（借用 suitBanner）→ 停止生產雜兵
  → 頭目從隨機一側滑入（170px/s，**進場中無敵**避免還沒登場就被秒殺）→ 錨定於該側
  → 攻擊循環 → 擊殺後 `bossBattle=null`、清空敵方彈幕、恢復生產、14顆結晶（位置夾回場內）
- **截斷側處理（玩家特別要求）**：素材皆面向左、截斷側（Kraid尾巴等）在右；右側登場不翻面、
  左側登場水平翻面，錨點 `±wpx*0.06` 讓截斷側永遠超出畫面邊緣，畫面上看不到斷面
- **各頭目體型與攻擊模式**（`BOSS_TABLE` 新增 heightFrac/grounded 欄位；HP 大幅上調 5000/9000/14000/22000）：
  - KRAID（5分，0.70H 站地）：三連腹部尖刺（瞄準玩家±0.14散布）＋全場隨機落石（拋物線，gravity 330）
  - CROCOMIRE（10分，0.48H 站地）：沿側邊上下巡弋（±0.2H正弦）＋5發扇形火彈
  - PHANTOON（15分，0.60H 漂浮）：每6秒淡出→瞬移換邊→淡入（alpha動畫，淡出時停止攻擊）＋10發環形幽靈火球
  - RIDLEY（20分，0.44H 漂浮）：雙發火球（瞄準玩家）＋每7秒鎖定玩家高度→0.8秒預警（紅色爆發）
    →720px/s俯衝橫掃全場→衝出畫面後換邊重新進場
- **敵方彈幕系統（全新）**：`enemyProjectiles`（上限90），4種彈型各有專屬繪製
  （spike尖刺旋轉三角／rock不規則岩石／fire閃爍火球／ghost半透明幽靈彈），
  重力支援、命中玩家走 `applyDamageToPlayer`＋動力服減傷＋0.5秒無敵
- **UI**：巨型頭目改用畫面頂部大血條＋名稱（不再用頭上小血條）；冰凍會暫停頭目行動（獎勵冰系）
- **互斥保護**：前一場頭目戰未結束時延後下一場；終極頭目與中頭目戰互斥（先打完中頭目）
- 全部瀏覽器實測：四隻頭目進場/攻擊/瞬移/俯衝/擊殺/恢復生產零錯誤
- **平衡註記**：HP 依「該時間點玩家尚無進化武器」估算，若玩家回饋頭目太快死/太肉，調 `BOSS_TABLE` hp 即可

### 2.11 第十二輪（v1.2：薩姆斯造型重製）
- 從圖鑑參考圖左上角的薩姆斯站姿取樣（abs 座標 (5,258)-(70,352)），
  以 HSV 色相分類器把取樣顏色**映射回動力服調色盤字母**（紅→R primary、橘/黃→O armor、
  高亮黃→Y highlight、綠→頭部G visor/其他N cannon、橄欖暗部→W trim、灰→E joint、深色→K），
  **四套動力服（Power/Varia/Gravity/Hyper）變色系統完全不變**
- 新矩陣 26×43（舊為 27×29），`SAMUS_UNIT 1.55→1.05`（畫面高度維持約45px、體型變修長），
  `SAMUS_LEG_ROW 22→32`（行走動畫分界對齊新腿部位置），翻面邏輯不變
- 產生器腳本：scratchpad/samus_gen.py（含四套配色並排預覽輸出）
- **頭目戰設計備忘（玩家 2026-08-11 提出，動工時必看）**：中頭目改為佔滿畫面一側的巨型頭目戰；
  **Kraid 的圖鑑素材右側尾巴是被截斷的，玩家明確表示不想在畫面上看到截斷破綻——
  設計上必須把截斷側貼齊畫面邊緣（斷面永遠在畫面外）**，其他頭目素材同理檢查

### 2.10 第十一輪（v1.1：精英配色重製／自爆警告／終極頭目保底與效能）
> 玩家對 v1.0 回饋：「雜兵辨識度超棒」「效能很順」——像素圖重製驗收通過。本輪處理其餘回饋：

- **精英變體重製**：移除金圈，改為「鍍金配色」（調色盤整體往 `#ffd24a` 混50%，預渲染 `|e` 變體）＋
  體型改為 +20%（`sizeMult 1.35→1.2`，依玩家指定）
- **自爆蟲警告**：`explodeOnDeath` 敵人週期性紅閃（`sin(phase*2.5)>0.55` 時切換 `|w` 紅色變體），
  傳達「會爆炸」的視覺警告
- **終極頭目卡頓修復**：QUEEN METROID 原本是唯一每幀逐像素重繪的敵人（約700次fillRect），
  改為三層預渲染快取（base／紅色脈動層／受擊變白層），實測含頭目的場景平均每幀 0.47ms
- **終極頭目觸發保底**：原本只靠「6把武器全進化」觸發，若玩家選的武器/被動組合湊不出6組進化對，
  永遠看不到結局——新增 **25分鐘（1500s）強制觸發**；且 `pendingFinalBoss` 現在在 `update()` 內
  也會被消化（原本只在選單流程中處理，時間觸發會卡住不生效）
- **終極頭目戰期間停止生產雜兵**（玩家提議），存活的雜兵不清除、由玩家自行清掉
- 全部經瀏覽器數據驗證：零錯誤、變體快取正確、25分鐘觸發公告正常、鍍金混色數值精確

### 2.9 第十輪（v1.0：全敵人像素圖重製，取樣自使用者提供的圖鑑）
- 使用者提供「Creatures of Zebes」圖鑑參考圖（放在 `ref/`，**已 gitignore，不會上傳到公開 repo**），
  以 Python/PIL 對 7 種雜兵 + 4 隻中頭目逐一裁切、分塊中位數降採樣、7色量化，
  產生像素矩陣（同薩姆斯的 rows+palette 格式），內嵌於 `ENEMY_SPRITES`
- **Puyo 的素材實際取自圖鑑的 BOYON（黃色尖刺球）**——圖鑑裡的 Puyo 圖太暗無法取樣，
  且 Boyon 的尖刺造型更符合「自爆蟲」的視覺語意
- **渲染架構**：`getSpriteCanvas(key, flash)` 把矩陣預渲染到離屏 canvas 快取
  （每種敵人2個變體：正常／受擊變白），每幀只做 `drawImage`＋`imageSmoothingEnabled=false`
  維持像素感；素材皆面向左，玩家在右側時水平翻面；縮放採「寬高分別擬合」
  `min(r*3.2/w, r*2.2/h)`，寬扁素材（Ripper）不會被壓成小點
- 精英金邊圈、冰凍濾鏡、頭目名條、血條機制全部不變
- **實測：400 隻敵人同屏 60fps、零錯誤**（預渲染貼圖比舊的向量繪製更快）；
  陳列室截圖確認 11 種敵人＋精英變體全部辨識度良好
- 取樣腳本在 scratchpad（extract1~5），方法已記錄；若要再加敵人，比照 2.7 節的測試法＋本節產生器流程

### 2.8 第九輪修正（v0.9.1：連鎖波動重新設計）——**已驗收通過**
> 玩家回饋（2026-08-10）：「波動光束終於有特色了，很成功」「最終頭目沒問題，也看得清楚了」
> ——連鎖波動新設計與 v0.9 的頭目修復、受擊變白效果均確認正常。

- 玩家回饋：進化後變成通用的綠色放射狀彈幕「沒有特色」。重新設計為**保留波動光束「並列光束牆」的識別**：
  - 光束間隔 9px→5.5px（更密）、每波 +4 條（`countAdd:4`）、冷卻 ×0.7、貫穿 +6
  - 發射方向不再跟隨玩家移動，改為**自動旋轉掃射**（每波前進 0.7 rad ≈ 40°，約 1.8 秒掃一整圈，
    `w.spinAngle` 累加實作，在 `fireDirectionalWave` 內以 `w.evolved` 分支處理，不再使用 `radial_burst` 分類）
  - 顏色從綠色 `#c8ffb0` 改回紫色系亮紫 `#e3b8ff`，延續本體識別；波浪擺動與緞帶彈頭保留
- `radial_burst` 分類仍保留給分光光束的進化（Prismatic Spazer）使用

### 2.7 第八輪修正（v0.9：自爆連鎖無限遞迴＝當機真正根因／升級佇列卡死／頭目消失結局被跳過）

這輪修的是**單一根因引發的連鎖災難**，由玩家回報「進化後波動光束消失」「大量經驗值後卡住、終極頭目消失、結局被跳過」追查而得：

- **根因：`killEnemy()` 的無限遞迴**。死亡的敵人要到函式結尾才從 `enemies` 陣列移除，但自爆蟲（Puyo）
  的死亡爆炸在移除前就執行——兩隻相鄰自爆蟲會互相重複殺死對方（A死→炸B→B死→炸A→A還在陣列裡→再殺A→…），
  造成 `RangeError: Maximum call stack size exceeded`，每幀重複拋出：
  - 例外中斷整幀更新＋繪製 → 畫面凍結、彈幕消失（玩家看到的「波動光束進化後不見了」多半是這個）
  - 錯誤累積 >200 幀後，自救機制 `enemies.length=0` **連終極頭目一起刪掉且不觸發勝利** → 結局被跳過、無限玩下去
  - **這也極可能是先前（2.2節）「當機」回報的真正根因**——當時歸因於 shadowBlur 是治標；
    環繞武器每幀掃過死而未除的敵人 → 每幀爆栈 → 看起來就是「畫面凍住」
  - 修法：`killEnemy` 開頭先設 `en.dead=true` 並**先從陣列移除**再做死亡特效；`damageEnemy` 開頭檢查 `en.dead` 直接返回。
    已在瀏覽器實測：8隻擠成一團的自爆蟲連環爆炸零錯誤
- **升級佇列卡死**：整合結晶一次給數千經驗值會排入 40 次升級，全滿級後每次升級顯示 0.9 秒的空卡片畫面
  →玩家被凍住 36 秒。修法：`openLevelUp()` 發現卡池全空時直接把整條 `levelUpQueue` 歸零、立即返回遊戲
- **狀態機加固**：`proceedAfterMenus()` 在 victory/gameover 狀態下直接返回（舊的公告計時器不能把結局畫面蓋掉）；
  新增 `gameRunId`，每局 +1，進化／頭目公告的 setTimeout 都檢查 runId，避免上一局殘留計時器污染新局
- **自救機制改為保留頭目**（`enemies.filter(e=>e.isBoss)`），且 `errorCount` 每局歸零（原本跨局累積，
  一局壞掉會讓之後每局的第一個錯誤就直接清場）
- **彈幕上限 260→420**：六武器全進化時力量光束（Nova Beam）獨佔 110+ 顆彈幕位，其他武器被擠掉看不見；
  提高上限後波動光束穩定保有 90+ 顆（已實測視覺清晰可見）
- **測試方法（之後可重用）**：複製 `index.html` 為 `test.html`（已在 .gitignore），在 `})();` 前注入
  `window.__dbg = {...}` 偵錯掛鉤（暴露 player/enemies/state 等 getter），用 `.claude/launch.json` 的
  python http.server 在瀏覽器面板載入，即可用 JS 直接構造武器配置／敵人場景／自動點卡片來重現與驗證 bug

### 2.6 第七輪修正（波動光束重做／受擊變白修正／經驗晶體整合／地雷拉力加強）
- **波動光束（Wave Beam）徹底重做**：原本是「瞄準最近敵人+角度扇形」，改成全新獨立分類 `directional`：
  - 方向改為**跟隨玩家最後移動方向**（`player.lastMoveAngle`，WASD組合自然對應8方位角度），不再鎖定敵人
  - 數量從 Lv1的1條線性增加到 Lv9的9條（`countEvery:1`），並列排列（垂直於發射方向偏移9px間距）而非角度扇形，
    視覺上呈現「越升級越粗」的效果
  - 顏色改為亮紫色 `#c86bff`（原本是綠色）
  - 進化後（Cascade Beam）維持原本的全方位放射邏輯（`radial_burst`），自動不需方向輸入，符合「進化後自動旋轉」的需求
- **修復「受擊變白」問題**：所有敵人（含終極頭目）原本受擊時會整隻變成純白色，蓋掉原本設計的外型。
  改成用 `mixWhite()` 輔助函式做「跟原色混合的變亮」而非直接取代成白色，保留敵人識別度。
  終極頭目的逐像素矩陣繪製也比照修正（每個像素依原色調亮，而非整隻變白）
- **經驗結晶整合機制（仿吸血鬼倖存者）**：畫面上一般結晶數量達到門檻（`GEM_CONSOLIDATE_CAP=45`）後，
  後續擊殺不再新增個別結晶，而是把經驗值累加進一顆紅色八芒星「整合結晶」（有 `+數字` 浮動文字顯示目前累積量），
  吃到會一次獲得大量經驗值。同時減少畫面上的結晶數量，對效能與視覺乾淨度都有幫助。頭目掉落的14顆結晶不受此機制影響（維持慶祝感）
- **磁力地雷拉力強化**：拉扯持續時間 0.45s→0.7s、拉力強度 140→260（且越靠近拉力越強）、
  影響半徑 blastRadius×1.6→×2.3，並加上拉扯中的粒子軌跡，讓「重力井」的吸入感更明顯

### 2.5 第六輪修正（平衡調整：頭目血量／敵人密度與強度）
- **終極頭目 QUEEN METROID 血量**：從 `16000*diff.hp*0.5` 大幅提高到 `90000*diff.hp*0.8`（約5-6倍），
  原本滿武器配置的薩姆斯5秒就能秒殺，現在應該有像樣的戰鬥時長
- **敵人數量上限**：420 → **600**（`MAX_ENEMIES`）
- **批次生成更積極**：每130秒（原180秒）門檻降低批次量，最多一次同時生成9隻（原6隻）
- **難度曲線整體調陡**（`DIFFICULTY_POINTS`）：HP/傷害/速度成長率全面提高，尤其中後期（10-20分鐘）
  與無盡模式（20分鐘後）成長更快，避免「武器太強、敵人一下死光看不出強度差異」的問題
- **精英變體提早出現**：從15分鐘後才有16%機率，改為8分鐘後即有機率（10%起跳，隨時間漲到32%封頂）
- 這輪是純數值調整，未變更任何邏輯結構，風險低

### 2.4 第五輪修正（bug修復 + 終極頭目 + 結局畫面）
- **修復**：電漿光束進化成電漿風暴後，畫面殘留一條連往舊目標的光束線 —— 根因是 `w.beamTarget` 在武器類別從
  `beam_continuous` 切換成 `orbit_field` 後沒有被清除，`drawContinuousBeams` 沒檢查目前類別就照畫。
  已修正為：只在類別仍是 `beam_continuous` 時才畫線，且進化當下主動清空 `beamTarget`。
- **強化視覺回饋**：抓鉤光束（Grapple Beam）原本只有線條特效、傷害不明顯，現在每個連鎖命中點都會噴粒子，
  進化版（Chain Lightning Array）額外留下短暫殘光。
- **強力炸彈（Power Bomb）機制調整**：不再固定在玩家腳下引爆，改為**隨機部署在戰場上**（第一發仍在玩家位置，
  之後幾發散布在周圍90-240px範圍），且等級提升會增加同時部署的數量（每3級+1發），進化版 Zero Countdown 額外+2發。
- **新增終極頭目與結局畫面**：
  - 條件：6把主武器全部進化完成（達成 Hyper Mode）時，觸發一次性「威脅偵測」公告畫面，
    短暫延遲後生成頭目 **QUEEN METROID**
  - 頭目造型：**依使用者上傳的參考圖，用 PIL 逐像素取樣重建**（技法同薩姆斯角色），
    黑底、綠色外殼、紅色觸手紋理、白色複眼，紅色部分有脈動發光動畫
  - 擊敗後觸發「MISSION COMPLETE」結局畫面（存活時間/等級/最終型態），可重新開始
  - 目前頭目沒有特殊攻擊模式，僅高血量＋高接觸傷害（跟其他4隻簡化頭目一致），是可以之後加強的方向

### 2.1 遊戲原型（`index.html`）—— 目前狀態：**武器/進化系統已上線，效能已優化，含終極頭目與結局**
- Canvas 2D、960x600 畫面，Crateria 風格背景（星空、遠山剪影、能量地板格線）
- 薩姆斯角色：依參考圖精確取樣重建的 27x29 像素矩陣，尺寸已縮小（`SAMUS_UNIT=1.55`）避免比敵人誇張的大
- 動力服變色系統：依「已完成進化的武器數量」自動觸發（2把→Varia、4把→Gravity、6把→Hyper），沿用閃光+粒子特效，額外有 suit-banner 文字提示
- **12種主武器 + 8種輔助能力的完整資料驅動系統**（`WEAPON_DEFS` / `PASSIVE_DEFS`），6+6欄位限制、9階/6階升級、抽卡池過濾邏輯皆已實作
- **12組進化**皆已實作，武器Lv9+搭檔被動Lv6自動觸發進化公告（全螢幕大字幕，非抽卡選擇）
- 9種武器行為類型各自有獨立邏輯：直線彈幕(各武器有專屬彈頭形狀:光柱/晶片/波浪緞帶/星芒)、持續光束、追蹤飛彈(火焰尾跡)、環繞光環(旋轉刀刃/電漿球團)、放置炸彈(閃爍引信+範圍預警圈)、延遲大爆炸、連鎖閃電、地雷(觸發範圍常駐虛線圈)、擴散脈衝
- 敵人：依時間分期難度曲線（線性插值）、7種敵人、精英變體、4隻簡化版頭目（Kraid/Crocomire/Phantoon/Ridley，目前只有高血量+觸碰傷害，無專屬攻擊模式）
- 升級卡片已優化：武器卡會顯示進化搭檔與其等級，被動卡會顯示「可進化哪些武器」，不限首次取得才顯示
- HUD 新增武器/被動欄位圖示列（emoji + 等級數字）

### 2.2 效能與穩定性（重要，避免重工）
> **2026-08-10 更新**：本節當時歸因的「當機」根因已被第八輪修正推翻——真正主因是自爆蟲互殺的無限遞迴
> （見 2.7 節），shadowBlur 只是加重症狀。以下保留原記錄供參考，shadowBlur 的效能教訓本身仍然有效。

- **已知問題與修復**：多個環繞型武器（Screw Attack + Plasma Storm evolved）同時存在、高等級(Lv40+)、340+敵人同時存在時，玩家回報「當機」（畫面凍結不動）。
  - **根因判斷**：並非邏輯錯誤或死循環，而是 Canvas `shadowBlur` 大量疊加（彈幕/環繞特效/粒子每幀重繪）造成瀏覽器分頁被拖到無回應，也可能伴隨未捕捉的例外導致 `requestAnimationFrame` 迴圈中斷。
  - **已完成的修復**：
    1. 移除高頻率繪製元素（彈道 `drawProjectiles`、環繞特效 `drawOrbitEffects`、地雷、經驗結晶）的 `shadowBlur`，只保留給低頻率效果（爆炸閃光環、連鎖閃電、持續光束、炸彈引信）
    2. `particles` 陣列加上上限 420、`projectiles` 陣列加上上限 260，超過會裁剪最舊的，避免無限增長拖垮效能
    3. **主迴圈 `loop()` 加上 try/catch 安全網**：任何一幀發生例外會被攔截並印到 console，不會再讓整個遊戲卡死不動；連續出錯超過200幀會自動清空敵人/彈幕自救
  - **如果之後又遇到類似「當機」回報**：先請使用者開瀏覽器 devtools 的 Console 分頁，看 `[Samus Survivors] frame error` 訊息內容，會直接告訴我們是哪一行出錯，比純粹肉眼除錯快很多
- 敵人數量已從 220 → 340 → **420**（`MAX_ENEMIES`），180秒後開始每次生成多隻（最多一次6隻）

### 2.3 設計文件（`weapon-evolution-design.md`）
已完成詳細規劃，內容包含：
- 6+6 欄位制（6把主武器／6個被動能力）、9階／6階升級、抽卡池過濾邏輯
- **12種主武器**清單與效果方向（Power Beam、Ice Beam、Wave Beam、Plasma Beam、Spazer Beam、
  Missile、Bomb Trail、Power Bomb、Screw Attack、Grapple Beam、Spider Mine、Echo Pulse）
- **8種輔助能力**清單（Energy Tank、Magnet Core、Reserve Tank、Booster Coil、
  Multi-Lock Module、Overload Capacitor、Piercing Core、Critical Sensor）
- **12組進化配對**與進化武器效果（例如 Power Beam+Overload Capacitor → Nova Beam）
- 敵人強度時間曲線（0-20分鐘+無盡模式，5隻頭目：Kraid/Crocomire/Phantoon/Ridley 等）
- 動力服變色改為「自動」依「已完成進化的武器數量」觸發（2把→Varia、4把→Gravity、6把→Hyper）

**這份設計文件已經是使用者確認過的最終版本，且核心系統已於 2.1 節完成實作。**

---

## 3. 下一步待辦（尚未開始／進行中）

大架構已完成，剩下是**打磨與平衡**：

1. [x] ~~重構武器資料結構~~ → 已完成
2. [x] ~~實作 6+6 欄位限制與抽卡池過濾邏輯~~ → 已完成
3. [x] ~~實作進化判定邏輯~~ → 已完成
4. [x] ~~實作動力服自動變色邏輯~~ → 已完成
5. [x] ~~實作 12 種主武器的基礎邏輯~~ → 已完成
6. [x] ~~實作 12 種進化武器的強化版邏輯~~ → 已完成（進化視覺目前僅換色+外圈光暈+行為強化，尚未做到「完全不同的形狀」，使用者反饋可接受但仍有加強空間）
7. [x] ~~中頭目專屬攻擊模式~~ → v1.3 完成；~~QUEEN METROID 重製~~ → v1.6 完成（見 2.15 節）
7b. [x] ~~薩姆斯造型升級~~ → v1.2 已完成（見 2.11 節）
8. [x] ~~「Metroid 警報」（吸血 Metroid 群）~~ → v1.6 以「QUEEN 產卵幼體」形式實現
       （若之後想做獨立的定時警報事件，`spawnMetroid()` 已可直接複用）
8b. [x] ~~背景美化~~ → v1.7 完成（見 2.16 節）
8c. [x] ~~武器與經驗晶石視覺美化~~ → v1.8 完成（見 2.17 節）
8d. [ ] **Godot 移植（玩家再次確認意向）**：玩家 2026-08-11 明示「你認為我們需要引擎時
       要記得說，那我們就移植到 Godot 去開發，離 Steam 上架就更近一步」——
       Claude 應持續評估第 5 節的移植訊號並主動提出時機
9. [ ] 8種輔助能力目前使用者回饋「除了磁力核心，其他不太有感」，需要之後檢視數值是否要加大效果或加視覺回饋（例如超載電容觸發時武器有明顯變化）
10. [ ] 持續依實際試玩回饋做平衡微調（目前已根據回饋調整過：經驗值曲線、水晶數值、敵人數量上限、UI字級、武器視覺形狀、炸彈/地雷效果與消失邏輯、效能優化）

**建議下次對話開場白**：附上這份交接文件 + 最新的 `index.html`，並說明「我要繼續哪一項待辦」或「這是新一輪的試玩回饋」。

---

## 4. 過程中的重要決策記錄（避免重工或方向反覆）

- 平台選擇：確定用**網頁版**而非 Pygame，理由是可即時預覽、易分享、視覺迭代快
- 角色美術：**不是手繪風格猜測，而是用 Python (PIL) 對使用者上傳的參考圖做逐像素取樣分析**，
  確保外觀精確還原，這個方法如果之後要新增其他角色/敵人素材也可以比照辦理
- 角色尺寸：經過三次調整（4→2.7→2.0→1.55），理由是「跟敵人比例不協調，看起來像玩家是魔王」
- 動力服視覺系統是使用者特別重視、想保留的賣點，**任何武器系統的改版都不能犧牲這個變色機制**；
  且此機制已與「武器進化數量」（2/4/6把）直接掛勾，取代了舊版需要手動選卡的方式
- **強力炸彈（Power Bomb / Zero Countdown）引爆時的全螢幕白閃已移除**（使用者反饋傷眼），
  但動力服變身的白閃特意保留（使用者明確表示喜歡）——**這是刻意的差異化決定，之後若要新增爆炸特效，
  預設不要用全螢幕白閃，除非是動力服變身這種「稀有、值得慶祝」的時刻**
- 效能教訓（重要）：**Canvas 的 `shadowBlur` 在高頻率繪製元素上（彈道、環繞武器特效等每幀都畫的東西）
  成本很高，容易在敵人/彈幕數量多時拖垮效能造成類當機現象**。日後新增視覺效果時，
  高頻率繪製的元素應優先考慮用透明度疊色而非 shadowBlur 做發光感，shadowBlur 保留給低頻率的
  一次性特效（爆炸、進化公告等）
- 主迴圈已加上 try/catch 安全網，日後即使程式碼有 bug 導致單幀出錯，也不會整個游戏卡死，
  瀏覽器 console 會印出 `[Samus Survivors] frame error` 方便除錯

- **版本標記機制（重要，每次交付新版都要做）**：開始畫面左下角有一行小字顯示版本號，
  來源是程式碼裡的 `const GAME_VERSION = '...'`（在 `<script>` 開頭附近）。
  **之後每次修改完 `index.html` 準備交付新版時，必須同步更新這個字串**
  （格式建議：`v版本號 · 日期 · 本次改動摘要`），這樣使用者上傳到 GitHub Pages 後，
  只要看開始畫面左下角文字，就能立刻確認朋友玩到的是不是最新版，不用每次都猜是不是要 Ctrl+F5。
  目前版本：`v1.9 · 2026-08-12 · 8-bit音效系統＋UI像素風重製（Super Metroid配色）`
  （對應第十九輪，2026-08-12 完成：
  **① 8-bit 音效系統**：`SFX` 模組（Web Audio 振盪器+雜訊緩衝，無音檔），14種音效
  （射擊/飛彈/命中/撿晶石/升級/選卡/進化/變身/頭目警報/爆炸/受傷/頭目擊殺/勝利/失敗），
  每種有最小間隔防洗版，`SFX.init()` 在開始按鈕手勢時建立，**M 鍵靜音**；
  掛鉤共19處（腳本 scratchpad/sfx_hooks.py 批次插入）。
  **② UI 像素風重製（依 ref/68580f46...jpg Super Metroid 裝備畫面）**：
  橘面板#e8a020+黑字+亮/暗雙內框+硬陰影（.sm-panel 語言）、紅點●條列（卡片）、
  黑底+深紫格線紋（所有overlay）、HP改粉紅能量格/XP綠格（repeating-gradient，
  注意 draw() 會逐幀覆寫 hpBar 內聯樣式、已同步改）、頭目警告紅面板閃爍、
  進化公告粉紅標題面板、勝利金面板/失敗紅面板。
  computed-style 驗證全部生效、零錯誤。
  **音效的實際聽感需玩家實測**（無頭測試聽不到），若音量/音色不對可調 RECIPES。
  **v1.9.1（第二十輪，依10項回饋修正）**：①開始畫面說明改透明深底（橘面板太像軟體對話框）
  ②卡片重製=深色透底+紫光條框+標題騎上框線+hover橘色「啟用」狀態（參考圖的黃底=啟用態）
  ③提示文字溢出XP框修正（xp-wrap上移/hint縮小） ⑥選卡確認音改雙音上行更醒目
  ⑦警報改4循環鋸齒波警笛、中頭目警告1.8→3.0s、QUEEN 2.6→3.4s ⑧總音量0.22→0.45
  ⑨磁力地雷萊姆綠→琥珀金#ffb830（苔蘚地上隱形問題） ⑩進化武器配色去白化：
  nova金橘/absoluteZero冰藍/plasmaStorm洋紅/prismatic檸黃/superMissile橘紅/chainReaction橙/
  zeroCountdown亮黃/shinespark粉紫/chainLightning電光青/gravityWell紫/resonance青綠。
  **v2.0（第二十二輪）**：④20個像素圖示完成——`ICON_DEFS`（12x12手繪字母矩陣）+
  `iconURL/iconHTML`（離屏canvas→dataURL快取），替換槽位列/升級卡片/進化公告三處emoji
  （emoji保留為fallback）。**圖示為初稿，個別不滿意可指名重繪**。
  ⑩進化特效深度強化：進化彈幕通用拖尾粒子、電漿風暴旋轉電弧、稜光散射七彩稜鏡彈
  （5色循環）、閃光衝刺刀刃殘影、零度射線目標冰晶飛濺。
  驗證：20圖示全渲染、進化武器混合實戰200幀零錯誤。
  **v2.1（第二十三輪）**：✔ 圖示解析度議題解決——玩家提供 `ref/weapon.png`（12格）與
  `ref/power.png`（8格）參考圖，**編號正好對應 WEAPON_ORDER / PASSIVE_ORDER**。
  以取樣管線（腳本 scratchpad/icons16.py：格子內縮7.5%避開框線→遮蔽左上編號→
  亮部bbox補正方形→16x16分塊中位數取樣→8色量化）重繪全部20個圖示並替換 ICON_DEFS；
  iconURL 畫布改為依矩陣尺寸。教訓：**取樣格狀圖鑑時必須內縮避開格線，否則bbox會拾到外框**。
  **🚀 Godot 移植已啟動（2026-08-12，HTML v2.1.1 功能凍結）**：
  計畫見 GODOT-MIGRATION.md。玩家已裝 Godot 4.7.1。
  M0+M1 完成：godot/ 專案骨架（960x600、Nearest過濾、GL Compatibility）＋
  78個PNG資產（export_assets.py 從 index.html 矩陣解析匯出）＋
  Player.gd（WASD移動/跑步10幀15fps/翻面/呼吸/邊界，手感參數照搬HTML版）。
  **下一步 M2**：敵人與生成（7雜兵+精英+難度曲線+晶石+升級選單）。
  HTML 版此後只修bug不加功能。開發循環：Claude寫檔→玩家Godot按F5→回報。
  **v1.9.2（第二十一輪）**：①絕對零度改為持續射線（evo加category:'beam_continuous'+range:300，
  weaponStats以damage×2.4換算dps，tickContinuousBeam持續凍結目標、巨型頭目走freezeCd防永凍；
  射線用進化冰藍色+白芯）；被凍結敵人蓋透明藍圓形標示（drawEnemies）
  ②總音量0.45→0.9 ③連鎖閃電與持續光束改「以敵人身體邊緣計距」——原本以中心點計距，
  巨型頭目（r≈110+）永遠搆不到，這就是玩家「頭目戰沒看過閃電」的根因。
  驗證：零度射線鎖定頭目/傷害/凍結、連鎖150px外連到頭目，零錯誤）

---

## 5. 引擎移植規劃（Godot / Steam 上架）—— 目前結論：**先不移植**

使用者詢問過何時該把專案移植到遊戲引擎（Godot）以利未來上架 Steam。討論結論：

- **暫緩移植**，原因：HTML 版本改動成本低（改一行程式碼馬上能測），現在遊戲設計仍在快速迭代驗證階段，
  過早移植到 Godot（場景/腳本拆分成多檔案，單次迭代變慢）會拖慢驗證速度，可能做白工
- **移植訊號**（之後任一項明顯出現時，可以重新評估）：
  1. 玩家（使用者）自己覺得「這就是我要的遊戲」，核心手感/內容/平衡都滿意了
  2. 效能瓶頸頻繁出現且已無法用目前手法（陣列上限、減少shadowBlur等）緩解
     （目前已知偶發的短暫卡頓，判斷是 JS 垃圾回收造成，屬於正常現象，暫不視為急迫訊號）
  3. 使用者明確想要存檔系統、成就、或有上架 Steam 的具體時間表
- **屆時的建議做法**：
  - Godot 是合適的選擇（免費開源、2D支援完整、GDScript易懂、內建 Steamworks 外掛可匯出 Steam 版本）
  - 現有的 `weapon-evolution-design.md` 可直接當作 Godot 專案的設計藍圖，不用重新設計
  - 移植階段建議改用 **Claude Code**（在使用者自己電腦上直接操作整個專案資料夾），
    不適合繼續在這種單檔案聊天視窗裡逐行編輯
  - 屆時可以先請 Claude 產出一份移植計畫文件（場景結構、腳本拆分方式、美術資產轉換方案）再開始動工

**Claude 日後應主動留意上述訊號，適時重新向使用者提出「現在是移植的好時機」的建議，而非被動等待詢問。**

---

## 6. 檔案清單

專案資料夾：`C:\Users\kento\OneDrive\桌面\Samus Survivors`

| 檔案 | 說明 | 狀態 |
|---|---|---|
| `index.html` | 可玩遊戲原型 | 武器/進化系統核心完成，持續依回饋打磨中 |
| `weapon-evolution-design.md` | 武器與進化系統設計文件 | 已確認定案，已實作 |
| `PROJECT-STATUS.md`（本檔案） | 跨對話交接摘要 | 每輪修改後會持續更新 |

（原本的 `feedback-template.md` 試玩回饋整理表已不存在；回饋目前直接在對話中口述整理即可，
若之後想恢復固定格式再重建。）
